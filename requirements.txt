# Root-level requirements.txt for convenience
# Uses CPU-only torch to keep Railway builds fast.
-r requirements-railway.txt

# ML/analysis extras
matplotlib>=3.8.0
statsmodels>=0.14.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
